project:
  type: manuscript
  # output-dir: docs

manuscript:
  article: index.qmd

format:
  html:
    comments:
      hypothesis: true
  docx: default
  typst:
    toc: true
    columns: 2
  jats: default

  # (other formats)
  # pdf: default

execute:
  freeze: true